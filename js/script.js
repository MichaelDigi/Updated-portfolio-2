VanillaTilt.init(document.querySelectorAll(".card"), {
    max: 25,
    speed: 400,
    glare: true,
    "max-glare": 1,
});

VanillaTilt.init(document.querySelectorAll(".features__box"), {
    max: 25,
    speed: 400,
    glare: true,
    "max-glare": 1,
    reverse: true
});

